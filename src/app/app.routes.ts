import { Routes } from '@angular/router';
import { PersonaComponent } from './persona/persona.component';

export const routes: Routes = [
    {path : 'persona', component : PersonaComponent}, // Ruta cuando el path es empleado
];
